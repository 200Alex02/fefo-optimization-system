# Конфигурация подключения backend к PostgreSQL
# В Docker Compose используйте DB_HOST=db; при запуске PostgreSQL локально — localhost.
DB_HOST=localhost
DB_PORT=5432
DB_NAME=fefo_optimization_db
DB_USER=fefo_user
DB_PASSWORD=fefo_demo_password
